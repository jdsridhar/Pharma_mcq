/** Metadata keys + cookie config for the Identity domain. */

/** Reflector key set by `@Roles()`. */
export const ROLES_KEY = 'identity:roles';

/** Reflector key set by `@Permissions()`. */
export const PERMISSIONS_KEY = 'identity:permissions';

/** Name of the httpOnly cookie carrying the refresh token. */
export const REFRESH_COOKIE_NAME = 'pmcq_refresh';
